#include <iostream>
using namespace std;
int main() {
    cin.tie(NULL);
    cout.tie(NULL);
    ios_base::sync_with_stdio(false);
    int up, down, total, day=1, sum=0;
    cin >> up >> down >> total;
    totla = total/(up-down);
    return 0;
}
